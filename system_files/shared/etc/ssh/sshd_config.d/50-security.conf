# SSH security hardening for greatlem0n-os
# Requires key-based authentication only

PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes

# Accept locale and terminal environment variables from SSH clients
# Required for Shellfish and other modern SSH clients
AcceptEnv LANG LC_*
